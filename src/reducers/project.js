const initialState = {
    items: [
        {name: 'Buildateam',},
        {name: 'Kickstagram'}

    ]
};

function projects(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}

export default projects;